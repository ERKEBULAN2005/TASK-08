Вариант 1

Уровень A: Сделать кнопку, которая меняет цвет формы при нажатии.

using System;
using System.Drawing;
using System.Windows.Forms;

namespace Erkebulan210
{
    public partial class Form1 : Form
    {
        Random rnd = new Random();

        public Form1()
        {
            InitializeComponent();
        }

        private void button1_Click(object sender, EventArgs e)
        {
            this.BackColor = Color.FromArgb(
                rnd.Next(256), 
                rnd.Next(256),
                rnd.Next(256)
            );
        }
    }
}

Уровень B: Сделать форму, которая при сворачивании (Resize) выводит сообщение «Форма свернута».

using System;
using System.Windows.Forms;

namespace Erkebulan210
{
    public partial class Form1 : Form
    {
        public Form1()
        {
            InitializeComponent();

            this.Resize += Form1_Resize;
        }

        private void Form1_Load(object sender, EventArgs e)
        {
            
        }

        private void Form1_Resize(object sender, EventArgs e)
        {
            if (this.WindowState == FormWindowState.Minimized)
            {
                MessageBox.Show("Форма свернута");
            }
        }
    }
}

Уровень C: Создать форму в виде круга (использовать Region). Добавить кнопку «Закрыть».

using System;
using System.Drawing;
using System.Drawing.Drawing2D;
using System.Windows.Forms;

namespace Erkebulan210
{
    public partial class Form1 : Form
    {
        public Form1()
        {
            InitializeComponent();
        }

        private void Form1_Load(object sender, EventArgs e)
        {
            GraphicsPath gp = new GraphicsPath();
            gp.AddEllipse(0, 0, this.Width, this.Height);
            this.Region = new Region(gp);
        }

        private void button1_Click(object sender, EventArgs e)
        {
            this.Close();
        }
    }
}